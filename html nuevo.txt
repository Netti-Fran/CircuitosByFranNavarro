<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Circuito • Fran Navarro</title>
  <link href="https://fonts.googleapis.com/css2?family=Barlow:wght@400;500;600;700&family=Barlow+Condensed:wght@700;800;900&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #0a0a0a;
      --card: #121212;
      --accent: #00ff9d;
      --fuerza: #c026d3;
      --complementos: #06b6d4;
      --core: #eab308;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      background: var(--bg);
      color: #ffffff;
      font-family: 'Barlow', sans-serif;
      min-height: 100vh;
      padding: 2rem;
      overflow: hidden;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2.5rem;
      max-width: 1400px;
      margin-left: auto;
      margin-right: auto;
    }

    .title {
      font-family: 'Barlow Condensed', sans-serif;
      font-size: 3.2rem;
      font-weight: 900;
      letter-spacing: -0.03em;
      text-align: center;
      flex: 1;
    }

    #fullscreen-btn {
      background: rgba(255,255,255,0.1);
      color: white;
      border: 2px solid rgba(255,255,255,0.2);
      width: 60px;
      height: 60px;
      border-radius: 50%;
      font-size: 1.6rem;
      cursor: pointer;
      transition: all 0.3s;
    }

    #fullscreen-btn:hover {
      background: var(--accent);
      color: black;
      transform: scale(1.1);
    }

    .bloques {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 2rem;
      max-width: 1400px;
      margin: 0 auto;
    }

    .bloque {
      background: var(--card);
      border-radius: 24px;
      padding: 2.2rem;
      border: 1px solid #222;
      height: fit-content;
    }

    .bloque-header {
      display: flex;
      align-items: center;
      gap: 16px;
      margin-bottom: 1.8rem;
    }

    .icon {
      width: 68px;
      height: 68px;
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2rem;
    }

    .fuerza .icon { background: #4c1d5e; color: var(--fuerza); }
    .complementos .icon { background: #164e63; color: var(--complementos); }
    .core .icon { background: #713f12; color: var(--core); }

    .bloque-title {
      font-family: 'Barlow Condensed', sans-serif;
      font-size: 1.85rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .subtitle {
      color: #888;
      font-size: 1.1rem;
      margin-top: 4px;
    }

    .ejercicio-principal {
      background: rgba(192, 38, 211, 0.12);
      border: 2px solid var(--fuerza);
      border-radius: 16px;
      padding: 1.8rem;
      text-align: center;
      margin: 1.5rem 0;
    }

    .rm {
      background: var(--fuerza);
      color: white;
      padding: 8px 24px;
      border-radius: 50px;
      font-size: 1.25rem;
      font-weight: 700;
      display: inline-block;
      margin-top: 10px;
    }

    .exercise-list {
      font-size: 1.35rem;
      line-height: 2.1;
    }

    .exercise-list li {
      padding: 10px 0;
      border-bottom: 1px solid #222;
    }

    .exercise-list li:last-child {
      border-bottom: none;
    }

    .optional {
      background: rgba(234, 179, 8, 0.1);
      border: 2px dashed var(--core);
      border-radius: 16px;
      padding: 1.8rem;
      margin-top: 1.5rem;
      font-size: 1.25rem;
    }

    .footer {
      text-align: center;
      margin-top: 4rem;
      color: #555;
      font-size: 1.1rem;
    }

    /* Modo TV - Fuentes más grandes */
    body.fullscreen {
      padding: 3rem;
    }

    body.fullscreen .title {
      font-size: 4rem;
    }

    body.fullscreen .bloque {
      padding: 2.8rem;
    }

    @media (max-width: 1100px) {
      .bloques { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <div class="header">
    <div></div>
    <div class="title">CIRCUITO DE ENTRENAMIENTO</div>
    <button id="fullscreen-btn" title="Pantalla Completa">⛶</button>
  </div>

  <div class="bloques">
    <!-- Bloque Fuerza -->
    <div class="bloque fuerza">
      <div class="bloque-header">
        <div class="icon">🏋️</div>
        <div>
          <div class="bloque-title">Bloque de Fuerza</div>
          <div class="subtitle">3 personas</div>
        </div>
      </div>
      <div class="ejercicio-principal">
        <strong style="font-size:1.5rem;">Peso Muerto</strong><br>
        <span class="rm">85% RM</span>
      </div>
      <p><strong>Grupo:</strong> 3 atletas</p>
      <p><strong>Intensidad:</strong> Alta</p>
    </div>

    <!-- Bloque Complementos -->
    <div class="bloque complementos">
      <div class="bloque-header">
        <div class="icon">🔄</div>
        <div>
          <div class="bloque-title">Circuito de Complementos</div>
          <div class="subtitle">4 personas • Rotación por estaciones</div>
        </div>
      </div>
      <div class="exercise-list">
        <ol>
          <li>1. Remo unilateral mancuerna</li>
          <li>2. Airbike</li>
          <li>3. Push press + Puente glúteo unilateral</li>
          <li>4. Farmer</li>
        </ol>
      </div>
    </div>

    <!-- Bloque Core -->
    <div class="bloque core">
      <div class="bloque-header">
        <div class="icon">🧘</div>
        <div>
          <div class="bloque-title">Bloque Core</div>
          <div class="subtitle">Opcional • Si sobra tiempo</div>
        </div>
      </div>
      <div class="optional">
        <strong>Ejercicios de Core o Cardio complementario</strong><br><br>
        • Planchas, abdominales, russian twists...<br>
        • Cardio complementario
      </div>
    </div>
  </div>

  <div class="footer">
    Centro de Entrenamiento Funcional • Fran Navarro
  </div>

  <script>
    const btn = document.getElementById('fullscreen-btn');
    const body = document.body;

    function toggleFullscreen() {
      if (!document.fullscreenElement) {
        document.documentElement.requestFullscreen().then(() => {
          body.classList.add('fullscreen');
          btn.innerHTML = '✕';
        });
      } else {
        document.exitFullscreen().then(() => {
          body.classList.remove('fullscreen');
          btn.innerHTML = '⛶';
        });
      }
    }

    btn.addEventListener('click', toggleFullscreen);

    // Salir con tecla Escape
    document.addEventListener('fullscreenchange', () => {
      if (!document.fullscreenElement) {
        body.classList.remove('fullscreen');
        btn.innerHTML = '⛶';
      }
    });

    // Atajo de teclado F11 o F
    document.addEventListener('keydown', (e) => {
      if (e.key === 'f' || e.key === 'F') {
        e.preventDefault();
        toggleFullscreen();
      }
    });
  </script>
</body>
</html>